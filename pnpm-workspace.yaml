# pnpm 11 起不再读取项目 .npmrc，工作区配置统一写在这里。
#
# 这里**刻意不写 storeDir**：pnpm store 的位置是"这台机器的事实"，
# 写进仓库就会把绝对路径带到别人的机器上（本项目开发时需要在工作区内放 store，
# 已改到本机用户级配置 ~/.npmrc；见工作区根目录的 TOPOSMITH-LOCAL-ENV.md）。
packages:
  - 'packages/*'
  - 'apps/*'

# esbuild（Vite 的依赖）的 postinstall 需要放行
allowBuilds:
  esbuild: true
