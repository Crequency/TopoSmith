# ⚠️ 本文件对 pnpm 11 **不生效** —— pnpm 11 已不再读取项目 .npmrc，
#    本项目的 pnpm 配置写在 pnpm-workspace.yaml（storeDir / allowBuilds）。
#    保留此文件只是为了给可能用到 npm 的场景留一份说明。
#
# 1) 会话环境里的代理是**过期值**：
#      http_proxy/https_proxy = http://192.168.1.161:40404 → No route to host
#    系统登录 shell 的正确配置在 /etc/profile.d/proxy.sh，实测可用：
#      http://10.0.30.81:40404
#    （2026-09-14 做过一次代理迁移，/etc 下留有 *.bak-pre-proxy-20260914 备份）
#    安装时要么显式绕过过期代理，要么指向正确代理：
#      env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY pnpm install
#      # 或：env https_proxy=http://10.0.30.81:40404 pnpm install
#
# 2) 本机 pnpm 默认 store 位于工作区之外且被沙箱置为只读，
#    必须使用工作区内的 store（见 pnpm-workspace.yaml 的 storeDir）。
registry=https://registry.npmjs.org/
